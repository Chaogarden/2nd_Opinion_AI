# 2nd Opinion AI - Dependencies
# ==============================

# Core Streamlit App
streamlit>=1.28.0
streamlit-mic-recorder>=0.0.8  # For in-browser audio recording

# Audio Transcription (WhisperX)
# Note: whisperx requires special installation, see README
faster-whisper>=0.10.0
pyannote.audio>=3.0.0  # For speaker diarization

# NLP / Medical NER
spacy>=3.5.0
scispacy>=0.5.0
# Note: Download scispacy models separately:
#   pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.1/en_core_sci_sm-0.5.1.tar.gz

# LLM Clients
openai>=1.0.0  # For OpenAI-compatible APIs
httpx>=0.24.0  # For async HTTP

# RAG / Embeddings
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
numpy>=1.24.0

# Data Handling
pydantic>=2.0.0
datasets>=2.14.0  # For ACI-Bench dataset loading

# SOAP Note Generation (omi-health/sum-small)
transformers>=4.36.0  # For AutoModelForCausalLM
accelerate>=0.25.0    # For device_map and memory efficiency

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
